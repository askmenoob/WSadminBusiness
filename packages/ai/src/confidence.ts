import type{AiIntentResult}from'./intent.js';
export type ConfidenceAction='EXECUTE'|'CLARIFY'|'HANDOFF';
export type ConfidenceDecision={action:ConfidenceAction;band:'HIGH'|'MEDIUM'|'LOW';reason:string;question:string|null};
const questions:Record<string,{ms:string;en:string}>={service:{ms:'Servis apa yang anda mahu?',en:'Which service would you like?'},date:{ms:'Tarikh bila yang anda mahu?',en:'Which date would you like?'},time:{ms:'Waktu bila yang anda mahu?',en:'What time would you prefer?'},location:{ms:'Lokasi/cawangan mana yang anda mahu?',en:'Which location or branch would you like?'},booking:{ms:'Tempahan yang mana satu?',en:'Which booking do you mean?'},pax:{ms:'Untuk berapa orang?',en:'For how many people?'}};
function band(v:number):'HIGH'|'MEDIUM'|'LOW'{return v>=.9?'HIGH':v>=.7?'MEDIUM':'LOW';}
function question(result:AiIntentResult){const key=result.missing[0];if(!key)return result.language==='en'?'Could you clarify your request?':'Boleh jelaskan sedikit permintaan anda?';const q=questions[key];if(!q)return result.language==='en'?`Please clarify ${key}.`:`Sila jelaskan ${key}.`;return result.language==='en'?q.en:result.language==='ms'?q.ms:`${q.ms} / ${q.en}`;}
export function decideConfidence(result:AiIntentResult):ConfidenceDecision{const b=band(result.confidence);if(result.intent==='HANDOFF')return{action:'HANDOFF',band:b,reason:'explicit_handoff',question:null};if(b==='LOW')return{action:'HANDOFF',band:b,reason:'low_confidence',question:null};if(result.missing.length)return{action:'CLARIFY',band:b,reason:`missing:${result.missing.join(',')}`,question:question(result)};if(b==='MEDIUM')return{action:'CLARIFY',band:b,reason:'medium_confidence',question:question(result)};return{action:'EXECUTE',band:b,reason:'high_confidence_complete',question:null};}
