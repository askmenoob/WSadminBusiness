import test from 'node:test';import assert from 'node:assert/strict';
import { DashboardService,DashboardValidationError,type DashboardRepository } from './index.js';
class Repo implements DashboardRepository{async snapshot(t:string,d='2026-08-31'){return{tenantId:t,locationId:null,localDate:d,timezone:'Asia/Kuala_Lumpur',rangeStart:new Date('2026-08-30T16:00:00Z'),rangeEnd:new Date('2026-08-31T16:00:00Z'),bookings:{total:5,pending:1,confirmed:2,completed:1,cancelled:1,noShow:0},utilization:{activeStaff:2,bookedMinutes:180,scheduledMinutes:960,percentage:18.75},pendingActions:{pendingBookings:1,noShows:0,attentionCount:1},recent:[],byLocation:[]};}}
test('dashboard returns daily business snapshot',async()=>{const svc=new DashboardService(new Repo());const r=await svc.get('t','2026-08-31');assert.equal(r.bookings.total,5);assert.equal(r.utilization.percentage,18.75);});
test('dashboard validates local date',()=>{const svc=new DashboardService(new Repo());assert.throws(()=>svc.get('t','31-08-2026'),DashboardValidationError);});
