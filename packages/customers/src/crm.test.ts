import test from'node:test';import assert from'node:assert/strict';import{CustomerCrmService,type CustomerCrmRepository}from'./crm.js';
class Repo{async createTag(t:string,n:string){return{id:'tag1',tenantId:t,name:n,createdAt:new Date()};}async listTags(){return[];}async setCustomerTags(){return[];}async addNote(t:string,c:string,a:string|null,n:string){return{id:'n1',tenantId:t,customerId:c,actorUserId:a,note:n,createdAt:new Date(),updatedAt:new Date()};}async listNotes(){return[];}async createField(t:string,i:any){return{id:'d1',tenantId:t,...i,active:true,sortOrder:0,createdAt:new Date(),updatedAt:new Date()};}async listFields(){return[];}async setFieldValue(_t:string,_c:string,d:string,v:any){return{definitionId:d,key:'vip_level',label:'VIP Level',fieldType:'TEXT' as const,value:v,updatedAt:new Date()};}async timeline(){return[];}async profile(){return{tags:[],notes:[],customFields:[],timeline:[]};}async recordEvent(_t:string,_c:string,i:any){return{id:'e1',type:i.eventType,sourceType:i.sourceType,sourceId:i.sourceId??null,title:i.title,detail:i.detail??{},occurredAt:new Date()};}}
test('CRM validates tags notes and custom field definition',async()=>{const svc=new CustomerCrmService(new Repo() as any);assert.equal((await svc.createTag('t',' VIP ')).name,'VIP');assert.equal((await svc.addNote('t','c','u',' follow up ')).note,'follow up');const f=await svc.createField('t',{key:'VIP Level',label:'VIP Level',fieldType:'TEXT'});assert.equal(f.key,'vip_level');});
test('CRM activity supports payment-compatible generic events',async()=>{const svc=new CustomerCrmService(new Repo() as any);const e=await svc.recordEvent('t','c',{eventType:'PAYMENT_RECEIVED',sourceType:'PAYMENT',sourceId:'p1',title:'Payment received',detail:{amountMinor:12000}});assert.equal(e.sourceType,'PAYMENT');});
