import test from'node:test';import assert from'node:assert/strict';import{CustomerControlError,CustomerControlService,type CustomerControlRepository}from'./restrictions.js';
class Repo implements CustomerControlRepository{async getRestriction(_t:string,c:string){return{customerId:c,blacklisted:false,reason:null,blacklistedAt:null,mergedIntoCustomerId:null,mergedAt:null};}async setBlacklist(_t:string,c:string,i:any){return{customerId:c,blacklisted:i.blacklisted,reason:i.reason,blacklistedAt:i.blacklisted?new Date():null,mergedIntoCustomerId:null,mergedAt:null};}async merge(_t:string,target:string,source:string){return{sourceCustomerId:source,targetCustomerId:target,moved:{bookings:1,conversations:1,notes:1,events:1,tags:1,customFields:1},mergedAt:new Date()};}}
test('blacklist state is validated',async()=>{const s=new CustomerControlService(new Repo());const r=await s.blacklist('t','c',{blacklisted:true,reason:' no show '});assert.equal(r.blacklisted,true);assert.equal(r.reason,'no show');});
test('customer cannot merge into itself',async()=>{const s=new CustomerControlService(new Repo());await assert.rejects(()=>s.merge('t','c','c',null),CustomerControlError);});
