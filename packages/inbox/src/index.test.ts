import test from'node:test';import assert from'node:assert/strict';import{contactE164,InboxService,type Conversation,type InboxMessage,type InboxRepository}from'./index.js';
const event:any={message:{remoteJid:'60123456789@s.whatsapp.net',participant:null}};
test('direct WhatsApp JID normalizes to E164',()=>assert.equal(contactE164(event),'+60123456789'));
class Repo implements InboxRepository{c:Conversation={id:'c1',tenantId:'t',instanceId:'i',customerId:'u',channel:'WHATSAPP',remoteJid:'x',contactE164:'+601',displayName:null,status:'OPEN',unreadCount:1,lastMessageAt:new Date(),lastMessagePreview:'hi',createdAt:new Date(),updatedAt:new Date()};m:InboxMessage[]=[];async project(){return null;}async listConversations(){return[this.c];}async getConversation(t:string,id:string){return t===this.c.tenantId&&id===this.c.id?this.c:null;}async listMessages(){return this.m;}async updateStatus(_t:string,_id:string,s:any){this.c.status=s;return this.c;}async markRead(){this.c.unreadCount=0;return this.c;}}
test('inbox service supports human takeover and read state',async()=>{const svc=new InboxService(new Repo());assert.equal((await svc.status('t','c1','HUMAN')).status,'HUMAN');assert.equal((await svc.markRead('t','c1')).unreadCount,0);});
