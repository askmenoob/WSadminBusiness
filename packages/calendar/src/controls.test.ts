import test from'node:test';import assert from'node:assert/strict';import{CalendarControlService,CalendarControlValidationError,type CalendarControlBlock,type CalendarControlRepository}from'./controls.js';
class Repo implements CalendarControlRepository{rows:CalendarControlBlock[]=[];async create(t:string,i:any){const r:CalendarControlBlock={id:`b${this.rows.length+1}`,tenantId:t,...i,staffId:i.staffId??null,resourceId:i.resourceId??null,createdAt:new Date()};this.rows.push(r);return r;}async remove(t:string,id:string){const n=this.rows.length;this.rows=this.rows.filter(x=>!(x.tenantId===t&&x.id===id));return this.rows.length<n;}async list(t:string){return this.rows.filter(x=>x.tenantId===t);}}
test('calendar controls validate target and recurrence',async()=>{const svc=new CalendarControlService(new Repo());const r=await svc.create('t',{scope:'STAFF',staffId:'s1',startsAt:'2026-08-31T02:00:00Z',endsAt:'2026-08-31T03:00:00Z',recurrence:'WEEKLY',recurrenceUntil:'2026-09-30T00:00:00Z'});assert.equal(r.recurrence,'WEEKLY');assert.throws(()=>svc.create('t',{scope:'STAFF',startsAt:'2026-08-31T02:00:00Z',endsAt:'2026-08-31T03:00:00Z'}),CalendarControlValidationError);});
