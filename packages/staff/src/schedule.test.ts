import test from 'node:test';import assert from 'node:assert/strict';
import { StaffScheduleService,StaffScheduleValidationError,type StaffScheduleRepository } from './schedule.js';
class Repo implements StaffScheduleRepository{weekly:any[]=[];overrides:any[]=[];blocks:any[]=[];async setWeeklyHours(_t:string,_s:string,i:any[]){this.weekly=i;return i;}async getWeeklyHours(){return this.weekly;}async addShiftOverride(t:string,s:string,i:any){const r={id:'o1',tenantId:t,staffId:s,...i,createdAt:new Date()};this.overrides.push(r);return r;}async listShiftOverrides(){return this.overrides;}async addTimeBlock(t:string,s:string,i:any){const r={id:'b1',tenantId:t,staffId:s,...i,createdAt:new Date()};this.blocks.push(r);return r;}async listTimeBlocks(){return this.blocks;}}
test('weekly hours reject overlap',async()=>{const svc=new StaffScheduleService(new Repo());await assert.rejects(async()=>svc.setWeeklyHours('t','s',[{weekday:1,startMinute:540,endMinute:720},{weekday:1,startMinute:660,endMinute:780}]),StaffScheduleValidationError);});
test('off-day override and leave block are accepted',async()=>{const svc=new StaffScheduleService(new Repo());const off=await svc.addShiftOverride('t','s',{localDate:'2026-08-31',isOff:true});assert.equal(off.isOff,true);const leave=await svc.addTimeBlock('t','s',{type:'LEAVE',startsAt:'2026-09-01T00:00:00Z',endsAt:'2026-09-02T00:00:00Z'});assert.equal(leave.type,'LEAVE');});
