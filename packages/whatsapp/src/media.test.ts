import test from'node:test';import assert from'node:assert/strict';import{EvolutionMediaResolver}from'./media.js';
test('Evolution media resolver uses provider message identity without exposing URL',async()=>{const original=globalThis.fetch;let url='',body:any=null;globalThis.fetch=async(input:any,init:any)=>{url=String(input);body=JSON.parse(String(init.body));return new Response(JSON.stringify({base64:Buffer.from('voice-bytes').toString('base64'),mimetype:'audio/ogg'}),{status:200,headers:{'content-type':'application/json'}});};try{const r=await new EvolutionMediaResolver('http://evolution:8080','test-key').resolve({providerInstanceName:'wsb-tenant',providerMessageId:'MSG123',mimeType:'audio/ogg',kind:'AUDIO'});assert.match(url,/\/chat\/getBase64FromMediaMessage\/wsb-tenant$/);assert.equal(body.message.key.id,'MSG123');assert.equal(Buffer.from(r.bytes).toString(),'voice-bytes');assert.equal(r.mimeType,'audio/ogg');}finally{globalThis.fetch=original;}});
