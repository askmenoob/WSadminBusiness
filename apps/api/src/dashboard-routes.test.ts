import test from 'node:test';import assert from 'node:assert/strict';import { buildApp } from './app.js';
import type { DashboardRepository } from '@wsadmin-business/dashboard';
process.env.DATABASE_URL??='postgresql://invalid:invalid@127.0.0.1:1/invalid';
class Repo implements DashboardRepository{async snapshot(t:string,d='2026-08-31'){return{tenantId:t,localDate:d,timezone:'Asia/Kuala_Lumpur',rangeStart:new Date('2026-08-30T16:00:00Z'),rangeEnd:new Date('2026-08-31T16:00:00Z'),bookings:{total:4,pending:1,confirmed:2,completed:0,cancelled:1,noShow:0},utilization:{activeStaff:2,bookedMinutes:180,scheduledMinutes:960,percentage:18.75},pendingActions:{pendingBookings:1,noShows:0,attentionCount:1},recent:[]};}}
const h=(tenant='ta')=>({'x-wsadmin-role':'TENANT_OWNER','x-wsadmin-tenant-id':tenant});
test('dashboard API returns business metrics',async()=>{const app=buildApp({dashboardRepository:new Repo()});const res=await app.inject({method:'GET',url:'/api/v1/tenants/ta/dashboard?date=2026-08-31',headers:h()});assert.equal(res.statusCode,200);assert.equal(res.json().bookings.cancelled,1);assert.equal(res.json().utilization.percentage,18.75);await app.close();});
test('dashboard API blocks cross tenant reads',async()=>{const app=buildApp({dashboardRepository:new Repo()});const res=await app.inject({method:'GET',url:'/api/v1/tenants/tb/dashboard?date=2026-08-31',headers:h('ta')});assert.equal(res.statusCode,403);await app.close();});
