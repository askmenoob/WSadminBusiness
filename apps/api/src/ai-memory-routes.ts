import type{FastifyInstance,FastifyReply,FastifyRequest}from'fastify';import{AccessDeniedError,ROLES,authorize,type Actor,type Role}from'@wsadmin-business/auth';import type{AiMemoryService}from'@wsadmin-business/ai';
function actor(r:FastifyRequest):Actor|null{const role=String(r.headers['x-wsadmin-role']??'')as Role;if(!ROLES.includes(role))return null;const tenantId=String(r.headers['x-wsadmin-tenant-id']??'');return{userId:String(r.headers['x-wsadmin-user-id']??'u'),role,...(tenantId?{tenantId}:{})};}
function guard(r:FastifyRequest,reply:FastifyReply,t:string){const a=actor(r);if(!a){reply.code(401).send({error:'authentication_required'});return false;}try{authorize(a,t,'TENANT_READ');return true;}catch(e){if(e instanceof AccessDeniedError){reply.code(403).send({error:'access_denied'});return false;}throw e;}}
export function registerAiMemoryRoutes(app:FastifyInstance,svc:AiMemoryService){app.get('/api/v1/tenants/:tenantId/ai/conversations/:conversationId/memory',async(r,reply)=>{const{tenantId,conversationId}=r.params as any;if(!guard(r,reply,tenantId))return reply;return{summary:await svc.getSummary(tenantId,conversationId),actions:await svc.listActions(tenantId,conversationId,50)};});}
