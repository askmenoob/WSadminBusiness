import test from'node:test';import assert from'node:assert/strict';import Fastify from'fastify';import{registerServiceOptionRoutes}from'./service-option-routes.js';import type{ServiceOption,ServiceOptionGroup,ServiceOptionRepository}from'@wsadmin-business/services';
class Repo implements ServiceOptionRepository{groups:ServiceOptionGroup[]=[];options:ServiceOption[]=[];async createGroup(t:string,s:string,i:any){const r={id:'g1',tenantId:t,serviceId:s,...i,createdAt:new Date(),updatedAt:new Date()};this.groups.push(r);return r;}async listGroups(){return this.groups;}async createOption(t:string,g:string,i:any){const r={id:'o1',tenantId:t,groupId:g,...i,createdAt:new Date(),updatedAt:new Date()};this.options.push(r);return r;}async updateOption(){return this.options[0]??null;}async listOptions(){return this.options;}}
const h={'x-wsadmin-role':'TENANT_OWNER','x-wsadmin-tenant-id':'t'};
test('service option API creates group and option',async()=>{const app=Fastify(),repo=new Repo();registerServiceOptionRoutes(app,repo);let r=await app.inject({method:'POST',url:'/api/v1/tenants/t/services/s/option-groups',headers:h,payload:{name:'Add-ons',selectionMode:'MULTIPLE'}});assert.equal(r.statusCode,201);r=await app.inject({method:'POST',url:'/api/v1/tenants/t/option-groups/g1/options',headers:h,payload:{name:'LED',durationDeltaMinutes:30,priceDeltaMinor:5000,requiredResourceType:'EQUIPMENT'}});assert.equal(r.statusCode,201);assert.equal(r.json().requiredResourceType,'EQUIPMENT');await app.close();});
