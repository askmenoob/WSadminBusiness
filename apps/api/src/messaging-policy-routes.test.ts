import test from'node:test';import assert from'node:assert/strict';import Fastify from'fastify';import{registerMessagingPolicyRoutes}from'./messaging-policy-routes.js';
const repo:any={async getPolicy(t:string){return{tenantId:t,timezone:'Asia/Kuala_Lumpur',quietStartMinute:1320,quietEndMinute:480,allowedWeekdays:[0,1,2,3,4,5,6],marketingEnabled:true,transactionalEnabled:true,maxMessagesPerMinute:30,updatedAt:new Date()}},async upsertPolicy(t:string,i:any){return{...(await this.getPolicy(t)),...i}},async getPreference(t:string,c:string){return{tenantId:t,customerId:c,whatsappEnabled:true,marketingOptIn:false,optedOutAt:null,updatedAt:new Date()}},async upsertPreference(t:string,c:string,i:any){return{...(await this.getPreference(t,c)),...i}},async countRecentDeliveries(){return 0},async auditDecision(){}};const h=(t='t1')=>({'x-wsadmin-role':'TENANT_OWNER','x-wsadmin-tenant-id':t});
test('messaging policy validates timezone and tenant scope',async()=>{const app=Fastify();registerMessagingPolicyRoutes(app,repo);let r=await app.inject({method:'PUT',url:'/api/v1/tenants/t1/messaging-policy',headers:h(),payload:{timezone:'Asia/Kuala_Lumpur',quietStartMinute:1320}});assert.equal(r.statusCode,200);r=await app.inject({method:'PUT',url:'/api/v1/tenants/t1/messaging-policy',headers:h(),payload:{timezone:'Mars/Base'}});assert.equal(r.statusCode,400);r=await app.inject({method:'GET',url:'/api/v1/tenants/t2/messaging-policy',headers:h()});assert.equal(r.statusCode,403);await app.close();});
