import type { FastifyInstance,FastifyReply,FastifyRequest } from 'fastify';
import { AccessDeniedError,ROLES,authorize,type Actor,type Role } from '@wsadmin-business/auth';
import { AvailabilityEngine,AvailabilityValidationError,type AvailabilityRepository } from '@wsadmin-business/availability';
function actorFrom(r:FastifyRequest):Actor|null{const role=String(r.headers['x-wsadmin-role']??'') as Role;if(!ROLES.includes(role))return null;const tenantId=String(r.headers['x-wsadmin-tenant-id']??'');return{userId:String(r.headers['x-wsadmin-user-id']??'dev-user'),role,...(tenantId?{tenantId}:{})};}
function guard(r:FastifyRequest,reply:FastifyReply,t:string){const a=actorFrom(r);if(!a){reply.code(401).send({error:'authentication_required'});return null;}try{authorize(a,t,'TENANT_READ');return a;}catch(e){if(e instanceof AccessDeniedError){reply.code(403).send({error:'access_denied',message:e.message});return null;}throw e;}}
export function registerAvailabilityRoutes(app:FastifyInstance,repo:AvailabilityRepository){const engine=new AvailabilityEngine(repo);
  app.post('/api/v1/tenants/:tenantId/availability/check',async(r,reply)=>{const{tenantId}=r.params as {tenantId:string};if(!guard(r,reply,tenantId))return reply;const body=(r.body??{}) as any;try{return await engine.check({tenantId,locationId:body.locationId,serviceId:String(body.serviceId??''),startsAt:body.startsAt,staffId:body.staffId,resourceId:body.resourceId,optionIds:Array.isArray(body.optionIds)?body.optionIds:undefined,excludeBookingId:body.excludeBookingId});}catch(e){if(e instanceof AvailabilityValidationError)return reply.code(400).send({error:'availability_validation',message:e.message});throw e;}});
}
