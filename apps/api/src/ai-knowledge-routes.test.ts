import test from'node:test';import assert from'node:assert/strict';import Fastify from'fastify';import{registerAiKnowledgeRoutes}from'./ai-knowledge-routes.js';
class Repo{rows:any[]=[];async search(){return[];}async createFaq(t:string,i:any){const r={id:'f1',tenantId:t,question:i.question,answer:i.answer,active:true,sortOrder:0,createdAt:new Date(),updatedAt:new Date()};this.rows.push(r);return r;}async listFaq(t:string){return this.rows.filter(x=>x.tenantId===t);}async updateFaq(){return null;}}
const h={'x-wsadmin-role':'TENANT_OWNER','x-wsadmin-tenant-id':'t1'};
test('tenant FAQ knowledge is writable only inside tenant',async()=>{const app=Fastify();registerAiKnowledgeRoutes(app,new Repo() as any);let r=await app.inject({method:'POST',url:'/api/v1/tenants/t1/ai/knowledge/faqs',headers:h,payload:{question:'Parking?',answer:'Level B2'}});assert.equal(r.statusCode,201);r=await app.inject({method:'POST',url:'/api/v1/tenants/t2/ai/knowledge/faqs',headers:h,payload:{question:'x',answer:'y'}});assert.equal(r.statusCode,403);await app.close();});
