import test from'node:test';import assert from'node:assert/strict';import Fastify from'fastify';import{registerAiSettingsRoutes}from'./ai-settings-routes.js';import type{AiSettings}from'@wsadmin-business/ai';
class Repo{v:AiSettings={enabled:true,primaryProvider:'GROQ',primaryModel:'g1',fallbackProvider:'OPENAI',fallbackModel:'o1',timeoutMs:12000};async get(){return this.v;}async update(_t:string,i:any){return this.v={...this.v,...i};}}
const h={'x-wsadmin-role':'TENANT_OWNER','x-wsadmin-tenant-id':'t1'};
test('AI settings are tenant-authorized and configurable',async()=>{const app=Fastify();registerAiSettingsRoutes(app,new Repo());let r=await app.inject({method:'PATCH',url:'/api/v1/tenants/t1/ai/settings',headers:h,payload:{primaryModel:'g2',timeoutMs:9000}});assert.equal(r.statusCode,200);assert.equal(r.json().primaryModel,'g2');r=await app.inject({method:'PATCH',url:'/api/v1/tenants/t2/ai/settings',headers:h,payload:{primaryModel:'bad'}});assert.equal(r.statusCode,403);await app.close();});
