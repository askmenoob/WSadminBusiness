import test from'node:test';import assert from'node:assert/strict';import Fastify from'fastify';import{registerCustomerCrmRoutes}from'./customer-crm-routes.js';
class Repo{async createTag(t:string,n:string){return{id:'tag1',tenantId:t,name:n,createdAt:new Date()};}async listTags(){return[];}async setCustomerTags(){return[];}async addNote(t:string,c:string,a:string|null,n:string){return{id:'n1',tenantId:t,customerId:c,actorUserId:a,note:n,createdAt:new Date(),updatedAt:new Date()};}async listNotes(){return[];}async createField(t:string,i:any){return{id:'d1',tenantId:t,key:i.key,label:i.label,fieldType:i.fieldType,active:true,sortOrder:0,createdAt:new Date(),updatedAt:new Date()};}async listFields(){return[];}async setFieldValue(_t:string,_c:string,d:string,v:any){return{definitionId:d,key:'x',label:'X',fieldType:'TEXT' as const,value:v,updatedAt:new Date()};}async timeline(){return[];}async profile(){return{tags:[],notes:[],customFields:[],timeline:[]};}async recordEvent(_t:string,_c:string,i:any){return{id:'e1',type:i.eventType,sourceType:i.sourceType,sourceId:null,title:i.title,detail:{},occurredAt:new Date()};}}
const h=(tenant='t1')=>({'x-wsadmin-role':'TENANT_OWNER','x-wsadmin-tenant-id':tenant,'x-wsadmin-user-id':'owner'});
test('customer CRM routes are tenant scoped',async()=>{const app=Fastify();registerCustomerCrmRoutes(app,new Repo() as any);let r=await app.inject({method:'POST',url:'/api/v1/tenants/t1/customer-tags',headers:h(),payload:{name:'VIP'}});assert.equal(r.statusCode,201);r=await app.inject({method:'POST',url:'/api/v1/tenants/t2/customer-tags',headers:h(),payload:{name:'bad'}});assert.equal(r.statusCode,403);await app.close();});
