import { useState } from 'react';
import { CalendarPreview } from './CalendarPreview';
import { DashboardPreview } from './DashboardPreview';
const modules=['AI Inbox','Calendar','Bookings','Customers','Staff','Services','Resources','Payments','Marketing','Reports','Settings'];
export function App(){const[active,setActive]=useState('Dashboard');return <div className="shell"><aside><div className="brand"><b>WS</b>admin <span>Business</span></div><nav><button className={active==='Dashboard'?'active':''} onClick={()=>setActive('Dashboard')}>Dashboard</button>{modules.map(m=><button key={m} className={active===m?'active':''} onClick={()=>setActive(m)}>{m}</button>)}</nav></aside><main>{active==='Dashboard'?<DashboardPreview/>:active==='Calendar'?<><header><div><p className="eyebrow">Booking operations</p><h1>Calendar</h1><p className="muted">Switch between staff and resource utilization without changing booking data.</p></div><div className="status">Daily / Weekly</div></header><CalendarPreview/></>:<section className="empty-state"><p className="eyebrow">{active}</p><h1>{active}</h1><p className="muted">Module shell reserved. Implementation follows the canonical TODO dependency order.</p></section>}</main></div>}
