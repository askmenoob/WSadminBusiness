import { useState } from 'react';
import { CalendarPreview } from './CalendarPreview';
const modules=['AI Inbox','Calendar','Bookings','Customers','Staff','Services','Resources','Payments','Marketing','Reports','Settings'];
function Dashboard(){return <><header><div><p className="eyebrow">WSadmin Business</p><h1>Business command center</h1><p className="muted">Repitte-class booking operations with an AI-first customer layer.</p></div><div className="status">Phase 1 · Booking Core</div></header><section className="cards"><article><small>Booking Engine</small><strong>Deterministic</strong><p>Capacity-safe service, staff and resource scheduling.</p></article><article><small>AI Receptionist</small><strong>Guarded</strong><p>Intent and confidence layer cannot write the database directly.</p></article><article><small>Calendar</small><strong>Operational</strong><p>Daily/weekly staff and resource timeline contract.</p></article><article><small>MVOC Safety</small><strong>Protected</strong><p>Separate repo, database, Redis and runtime.</p></article></section><section className="panel"><div><h2>Build contract</h2><p>AI interprets customers. Deterministic business services validate and execute bookings. Every lifecycle action is tenant-scoped and audited.</p></div><div className="flow"><span>WhatsApp / Web</span><b>→</b><span>AI Interpreter</span><b>→</b><span>Booking Service</span><b>→</b><span>PostgreSQL</span></div></section></>}
export function App(){const[active,setActive]=useState('Dashboard');return <div className="shell"><aside><div className="brand"><b>WS</b>admin <span>Business</span></div><nav><button className={active==='Dashboard'?'active':''} onClick={()=>setActive('Dashboard')}>Dashboard</button>{modules.map(m=><button key={m} className={active===m?'active':''} onClick={()=>setActive(m)}>{m}</button>)}</nav></aside><main>{active==='Dashboard'?<Dashboard/>:active==='Calendar'?<><header><div><p className="eyebrow">Booking operations</p><h1>Calendar</h1><p className="muted">Switch between staff and resource utilization without changing booking data.</p></div><div className="status">Daily / Weekly</div></header><CalendarPreview/></>:<section className="empty-state"><p className="eyebrow">{active}</p><h1>{active}</h1><p className="muted">Module shell reserved. Implementation follows the canonical TODO dependency order.</p></section>}</main></div>}
